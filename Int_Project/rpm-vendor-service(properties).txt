# MYSQL Properties
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rpm-vendor
    driver-class-name: com.mysql.cj.jdbc.Driver
    username:
    password:
    
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
  
  h2:
    console:
      enabled: true
  data:
    jpa:
      repositories:
        bootstrap-mode: default

  application:
    name: vendor-service
  
server:
  port: 8082